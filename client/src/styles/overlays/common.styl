#game_overlay,
#ui_overlay {
    position: absolute;
    z-index: 5;
    display: flex;
    width: 100dvw;
    height: 100dvh;
    pointer-events: none;
    align-items: center;
    justify-content: center;
}
