:stylus(wrap=true)
    #ui-targeting-list {
        position: absolute;
        top: 1vh;
        left: 1vw;
        z-index: 4;
        font-family: monospace;
        text-shadow: 1px 1px grey;
        background: rgba(0,0,0,0.25);
        padding: 0.5rem 1rem;
        resize: both;
        overflow: auto;
        color: #CCC;
        pointer-events: bounding-box;
        backdrop-filter: blur(2px);
        border: solid 1px rgba(0,0,0,0.25);

        .header {
            display: flex;
            padding: .5rem 0;
            img {
                margin: 0 1rem 0 0.5rem;
            }
            
        }

        button {
            background: rgba(1,1,1,0.1);
            border-radius: 16px;
            padding: 0.125rem 0.25rem 0;
            pointer-events: auto;
        }
        
    }
#ui-targeting-list
    .header
        img(src='/assets/ui/satellite-dish-solid.svg' width='16' height='16')
        strong Sensors
    table
        thead
            tr
                td Name
                td Type
                td Distance
                td 
        tbody
            include ./targeting/list_target.pug
            include ./targeting/list_target.pug
            include ./targeting/list_target.pug
            include ./targeting/list_target.pug
            include ./targeting/list_target.pug
            include ./targeting/list_target.pug
            include ./targeting/list_target.pug
            include ./targeting/list_target.pug
            include ./targeting/list_target.pug
            include ./targeting/list_target.pug
            include ./targeting/list_target.pug
