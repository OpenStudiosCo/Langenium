style.
    #ui-targeting-container {
        display: flex;
        position: absolute;
        background: rgba(0, 0, 0, 0.25);
        bottom: 1vh;
        left: 35vw;
        width: 30vw;
        height: 10vh;
        border-radius: 1% / 5%;
        z-index: 2;
        pointer-events: none;
    }

    .target {

    }
#ui-targeting-container
