	uniform float time;
	attribute float displacement;
	uniform float alpha;
	
	varying vec2 vUv;

	void main() {

		vUv = uv;

	    vec3 p = position;

	    p.y += sin(2.0 * p.y + time) * 222.654321;


	    vec4 mvPosition = modelViewMatrix * vec4(p, 1.0 );

	    gl_Position = projectionMatrix * mvPosition;
	}